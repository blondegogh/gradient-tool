
  # Gradient Builder Tool

  This is a code bundle for Gradient Builder Tool. The original project is available at https://www.figma.com/design/acchABQvaj4RXd5MjPSlnM/Gradient-Builder-Tool.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  